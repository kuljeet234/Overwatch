opencv-python>=4.8,<5.0
face-recognition>=1.3,<2.0
numpy>=1.24,<3.0

# face-recognition pulls dlib, which needs CMake and a C++ toolchain at build time:
#   macOS:  brew install cmake
#   Ubuntu: sudo apt-get install build-essential cmake
